{
  "name": "qr-code-service",
  "version": "0.1.0",
  "description": "Simple QR Code generator service.",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "start": "npm run dist && node dist/index",
    "dist": "./node_modules/.bin/babel src --out-dir dist",
    "dev": "./node_modules/.bin/babel-node src/index",
    "nodemon": "nodemon --exec npm run dev"
  },
  "author": "Steve Ottoz <so@dev.so>",
  "license": "MIT",
  "dependencies": {
    "@babel/preset-env": "^7.11.0",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "express": "^4.17.1"
  }
}
